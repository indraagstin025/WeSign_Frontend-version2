chore(documents): batch fix Low issues (L-1..L-8) + 3 bonus lint cleanup

Sprint Low Documents — refactor minor + dokumentasi + cleanup dead code.

L-1: Hapus 3 file orphan (dead code)
- DocumentHeader.jsx (tidak di-import siapa pun)
- DocumentToolbar.jsx (tidak di-import siapa pun)
- DocumentFilter.jsx (consumer DocumentToolbar juga orphan)
- DocumentsPage tetap pakai layout existing (filter bar + status tabs)

L-2: Hapus inline Loader2 component di EditDocModal
- Sebelumnya component custom Loader2 di-define di file yang sama
- Sekarang import Loader2 dari lucide-react (sudah dipakai di file lain)

L-3: Update JSDoc useDocumentInfo + hapus param `document` unused (Bonus #1)
- Param `document` tidak pernah dibaca di hook (lint no-unused-vars)
- Hook pure helper: getStatusConfig + formatDate
- JSDoc lengkap dengan @returns shape

L-4: Hapus dead helpers di useDocumentTable
- `getStatusStyles` dan `menuRef` tidak dipakai konsumer (DocumentTable.jsx)
- Switch implementation handleClickOutside ke `data-document-menu` selector
- JSDoc lengkap dengan @returns shape

L-5: Replace inline `<style>` dengan utility — file sudah dihapus di L-1
- DocumentFilter (yang punya inline <style>) adalah orphan, dihapus

L-6: Accessibility DocumentInfoModal
- role="dialog" + aria-modal + aria-labelledby di outer container
- aria-hidden di backdrop
- aria-label di X button + Download version button
- scope="col" di table headers
- aria-label="Daftar penanda tangan dokumen" di table

L-7: Konsistensi bahasa comment
- useUploadDoc.js: comment English -> Bahasa Indonesia konsisten
- useDocumentInfo.js: JSDoc Bahasa Indonesia
- useEditDoc.js: JSDoc Bahasa Indonesia

L-8: Centralize MAX_UPLOAD_BYTES + MAX_UPLOAD_LABEL
- New: constants/uploadLimits.js
- useUploadDoc import dari constants
- Pesan error tetap konsisten dengan label

Bonus #2: useEditDoc setState-in-effect anti-pattern
- Sebelumnya useEffect sync prop document -> state form (lint flagged)
- Solusi: parent pass key={document.id} ke EditDocModal -> React re-mount
  setiap dokumen berbeda -> state init dari prop saat mount, no effect

Bonus #3: useUploadDoc `err` unused di catch
- Sebelumnya catch (err) tidak dipakai (lint no-unused-vars)
- Sekarang log dengan createLogger('UploadDoc') untuk debugging

Verification:
- npm run build sukses (6.06s)
- npx eslint src/features/documents/ CLEAN (0 error)

Refs: docs/code-review-feat-documents/04-low.md L-1..L-8 + 3 bonus
